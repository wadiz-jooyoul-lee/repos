# 2026-04-26 일괄 Pull 업데이트 노트

분석 baseline 이후 18 repo 의 최신 브랜치를 fetch + pull 한 결과를 한 곳에 정리. 분석 문서가 어디에 영향 받는지 + 후속 작업 제안.

## 1. Pull 결과 요약

### 변경 없음 (6)
- `app-api`, `client-document`, `co.wadiz.fep`, `com.wadiz.api.reward`, `com.wadiz.wave.user`, `nicepay-api`
- 본 분석 baseline 이후 master/main 변경 없음

### 변경 있음 (12) — 약 165 커밋

| Repo | 브랜치 | 커밋 | 파일 | 영향 평가 |
|---|---|---|---|---|
| **wadiz-frontend** | master | 365 | 304 | 펀딩 상세 native 모듈 추가 (별도 분석 완료) |
| **wadiz-android** | main | 298 | — | 펀딩 상세 네이티브 (별도 분석 완료) |
| **wadiz-ios** | develop | 122 | — | 펀딩 상세 네이티브 (별도 분석 완료) |
| **com.wadiz.web** | master | 23 | 90 | RewardChangeLog · 약관 · SEO (별도 분석 완료) |
| **co.wadiz.api.community** | master | 36 | 224 | ⚠️ **Phase 0~1 → Phase 6 v1.3.0 풀 구현 완료** |
| **co.wadiz.currency-exchange** | dev | 24 | 32 | dev 브랜치 진화 |
| **com.wadiz.api.funding** | master | 15 | 20 | RWD-5479 i18n 알림 + asciidoc native 신규 |
| **kr.wadiz.account** | master | 7 | 8 | Apple webhook · 소셜 슬롯 edge case |
| **kr.wadiz.user.link** | master | 6 | 4 | Neo4j Docker 셋업 자동화 (RC4) |
| **com.wadiz.api.startup** | master | 6 | 9 | ProjectType 컨버터 · 페이징 · 메이커 등급 |
| **com.wadiz.adm** | master | 3 | 1 | 펀딩 일시중지 자동해지 차단 |
| **makercenter-be** | main | 3 | 9 | CLIENT-58 어드민 기획전 벌크 신청 |
| **makercenter-fe** | main | 10 | 14 | HeaderOnly 프레임 추가 |
| **makercenter-fe-admin** | main | 5 | 10 | BulkApplication 모듈 |
| **figma-icon-sync** | main | 4 | 827 | 아이콘 일괄 rename (자동 sync 결과) |
| **wadiz-claude-plugins** | main | 46 | 22 | shared/skills/create-story 외 신규 플러그인 |

전 repo dirty=0 → 모두 fast-forward 안전.

---

## 2. 가장 중요한 변화 — `co.wadiz.api.community`

**이전 분석** (`docs/co.wadiz.api.community.md`): Phase 0~1 스캐폴드, **도메인 코드 없음**, `@MapperScan` 가 가리키는 패키지 미존재.

**현재**: 풀 구현 (Phase 6 v1.3.0 완료).
- **Java 소스 202 파일** (이전 0)
- **37 REST endpoint, 10 Swagger Tag**
- 서비스 포트 `9011`
- Java 25 + `--enable-preview` (StructuredTaskScope JEP 505) + Virtual Thread + Jackson 3 + RestClient 단일화

### 모듈 구조
```
src/main/java/co/wadiz/community/
├── config/                    # middleware, properties, db
├── integration/               # mail, user, push, campaign, points, event
├── module/
│   └── supporter_signature/   # ★ 풀 구현된 첫 도메인 모듈
│       ├── controller/        # 11 controllers (V1/V3 통합 후 V3 만 유지 추정)
│       ├── service/
│       ├── repository/{entity, mapper, param}
│       ├── dto/{request, response}/{signature, point, affiliate, communication, keyword}
│       ├── integration/{cache, notification}
│       ├── component/, model/{constant, domain}, event/
└── shared/{wadiz, response, cache, util, common, error}
```

### 컨트롤러 목록
```
SupporterSignatureController              /api/v3/supporter-signatures
SupporterSignatureUserController          /api/v3/users/{userId}/supporter-signatures
SupporterSignatureCommunicationController /api/v3/supporter-signatures
SupporterSignatureCommunicationUserController /api/v3/users/{userId}/supporter-signatures
SupporterSignaturePointController         /api/v3/supporter-signatures/points
SupporterSignaturePointUserController     /api/v3/users/{userId}/supporter-signatures
SupporterSignatureAffiliateController     /api/v3/supporter-signatures
SupporterSignatureAffiliateUserController /api/v3/users/{userId}/supporter-signatures
SupporterSignatureInterestDegreeController /api/v3/supporter-signatures/interest-degree
SupporterSignatureKeywordController       /api/v3/supporter-signatures/keywords
```
→ wave.user 의 V3 컨트롤러 11개 (동일 경로) 가 community 로 1:1 이관 완료.

### 분석 문서 영향 — 보강 필요
- **`docs/_flows/supporter-signature.md`** (221줄) — `com.wadiz.web` RestTemplate 의 `baseUrl` 이 community(9011) 로 전환됐는지 wave.user 그대로인지 확인 필요. **두 백엔드가 한동안 공존할 가능성 높음**.
- **`docs/co.wadiz.api.community.md`** (98줄) — Phase 2 폴더 구조로 승격 가치 (community 가 이제 실 서비스).
- **`docs/com.wadiz.wave.user/api-details/signature-v3.md`** — community 이관 진행 상태 갱신 (이전엔 "Phase 0 스캐폴드", 이제 "이관 거의 완료").

---

## 3. `com.wadiz.api.funding` (15 커밋)

**RWD-5479 — 다국어 알림 강화**
- 프로젝트 노출 email projectUrl link 국내/글로벌 구분
- 프로젝트 공개/비공개 알림톡 Template 변경 + payload 확장
- AI 심사 RequestKey 에 언어 추가 (동시성 충돌 방지)
- POST_APPROVAL 심사 결과 수신 시 번역 요청
- 최종승인 후 AI 심사 요청 시 작성언어/옵션언어 기준
- Slack 알림에 언어 정보 추가
- Batch 번역 대상 조회 Query 수정

**구조 변경**
- `adapter/application/src/docs/asciidoc/native.adoc` 신규 — **네이티브 펀딩 상세 API 문서 추가** (`/api/funding/native/v1/projects/{id}` 의 BE 위치 확인 단서!)
- `domain/aireview/AIReviewController.java` + `AIReviewInternalController.java` 변경
- `domain/additionalservice/` 신규
- `domain/bottomsheet/BottomSheetProxy` 변경

### 분석 문서 영향
- **`docs/_flows/funding-detail-native.md`**: BE `/api/funding/native/v1/projects/{id}` 의 위치가 **com.wadiz.api.funding** 이라는 단서 확보 — `asciidoc/native.adoc` 추가가 그 증거. (이전엔 "추정" 으로 기록).
- **`docs/com.wadiz.api.funding/`** Phase 2: AI 심사·번역 i18n 영역 확장 — `aireview` 분석 갱신 가능.

---

## 4. `kr.wadiz.account` (7 커밋)
- Apple webhook 분리: `AppleNotificationController` + `AppleEventPayload`, `AppleServerNotificationEvent` DTO 추가
- BE3-310: 소셜 로그인 시 같은 provider 슬롯 점유 edge case 분리
- 소셜 계정 목록 연동 시점 오름차순 정렬
- FE1-421: `global-account` layout.html 에 sendWebMessage 브리지 함수 추가 (앱 ↔ 웹 메시지)
- `usercoupon/UserCouponAdapter`, `UserCouponClient`, `fastmail2/NormalMailClient` 변경

### 분석 문서 영향
- **`docs/kr.wadiz.account/api-details/inbound-controllers.md`**: AppleNotification 영역 갱신 (DTO 분리)
- **`docs/_flows/login.md`**: sendWebMessage 브리지 추가 → 앱 임베드 로그인 흐름 보강

---

## 5. `kr.wadiz.user.link` (6 커밋)
- BE3-315: Neo4j Docker 셋업 자동화 (`infra/neo4j-setup/`)
- RC4 Neo4j 환경 추가 (`.github/workflows/aws_deploy_ecr_rc4.yml`)
- S3 경로 `user-platform/*` prefix 로 통일
- ENV_NAME 환경변수 제거
- `kafka/message/MessageStoreProject.java` 변경 (스토어 이벤트 메시지 포맷)

### 분석 문서 영향
- 코드 영향 미미. 인프라 셋업 변화. 분석 문서는 그대로 유효.

---

## 6. `com.wadiz.adm` (3 커밋)
- RWD-5479: 프로젝트 일시중지 시 +14일에서 먼 미래 값으로 설정 → **자동 해지 차단**
- 1 파일만 변경 (소량)

### 분석 문서 영향 — 미미.

---

## 7. `com.wadiz.api.startup` (6 커밋)
- BE3-249: ProjectType path variable 대소문자 무관 처리 Converter 추가
- BE3-96: 페이징 offset 환산, RestTemplate 주입 방식 개선
- `MakerGradeAdapter`, `AwardMakerRepository`, `MakerPageAdminConverter` 변경

### 분석 문서 영향 — 미미.

---

## 8. `makercenter-be` (3 커밋)
- CLIENT-58: 어드민 기획전 대리 벌크 신청 기능 추가
  - `ExhibitionAdminController` 변경
  - `ReqBulkApplicationApply`, `ResBulkApplicationResult` DTO 추가
  - `ExhibitionApplicationAdminService` 추가
  - `migration/V004__exhibition_application_apply_manager_idx.sql` (인덱스 추가)
- CLIENT-58: CSV 다운로드에 utm 소스/미디엄/캠페인/콘텐츠/키워드 모두 표시

### 분석 문서 영향
- **`docs/makercenter-be.md`**: 어드민 기획전 벌크 신청 영역 추가 가치.

---

## 9. `makercenter-fe` / `makercenter-fe-admin`
- `makercenter-fe`: `HeaderOnly.jsx` 프레임 추가, 부수 변경 14 파일
- `makercenter-fe-admin`: `BulkApplicationMange/` 모듈 추가 (CLIENT-58 대응 — 벌크 신청 운영 화면)

### 분석 문서 영향 — 소량 보강 가치.

---

## 10. `co.wadiz.currency-exchange` (24 커밋, dev 브랜치)
실제 실시간 코드 작성 진행 중. dev 브랜치 단독 진화. master 는 여전히 README 만.
test 디렉터리 신규 추가 등 — 계속 적극 개발 중.

### 분석 문서 영향
- **`docs/co.wadiz.currency-exchange.md`** (137줄, dev 브랜치 분석) — 변경 폭 검증 필요. 핵심 컨트롤러 7 endpoint 는 안정적일 가능성.

---

## 11. `figma-icon-sync` (4 커밋)
아이콘 자동 sync 결과 — 827 파일 rename. 도구 동작 확인 (실 분석 영향 없음).

---

## 12. `wadiz-claude-plugins` (46 커밋)
새 plugin 다수:
- `plugins/shared/skills/create-story/` 등 신규
- 사내 Claude 워크플로우 보강

분석 영향 없음 (도구 repo).

---

## 후속 작업 제안 (우선순위)

### 🔴 권장 (분석 정확도 영향 큼)
1. **`co.wadiz.api.community` Phase 2 승격** — 단일 파일 → 폴더 구조. supporter_signature 모듈 11 컨트롤러 + integration + shared 분석.
2. **`docs/_flows/supporter-signature.md` 보강** — 백엔드 전환 (wave.user → community) 시점 검증. RestTemplate baseUrl 또는 routing 분기 확인.
3. **`docs/_flows/funding-detail-native.md` 보강** — `com.wadiz.api.funding/asciidoc/native.adoc` 읽어서 BE endpoint 명세 확정.

### 🟡 선택
4. `kr.wadiz.account` Apple Notification DTO 분리 반영 (소량)
5. `com.wadiz.api.funding` aireview/번역 영역 갱신 (Phase 2 보강)
6. `makercenter-be` 어드민 기획전 벌크 신청 추가
7. `co.wadiz.currency-exchange` dev 브랜치 분석 검증

### ⚪ 무시 가능
- adm·startup·user.link 인프라 변경
- figma-icon-sync 자동 rename
- wadiz-claude-plugins (도구)

---

**브랜치 상태**: 18 repo 모두 origin 과 동기화 완료. 작업 트리 깨끗.
